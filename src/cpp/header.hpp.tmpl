/* This file is generated by redf */

#pragma once

{{#each messages}}
{{/each}}
#include <std_msgs/msg/string.hpp>
#include <rclcpp/qos.hpp>

#include <string>

namespace {{namespace}} {

{{#each redf.endpoints}}
/**
 * {{description}}
 */
class {{pascal title}} {
public:
  using MessageType = {{cpp_type message_type}};

  static inline std::string topic_name() {
    static const std::string topic = "{{topic}}";
    return topic;
  }

  static inline rclcpp::QoS qos(rclcpp::HistoryPolicy history_policy = rclcpp::HistoryPolicy::SystemDefault, size_t depth = 10) {
    static const rclcpp::QoS qos = {{qos qos}};
    return qos;
  }
};

{{/each}}
}
