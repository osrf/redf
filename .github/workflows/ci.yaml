name: Test
on:
  pull_request:
  push:
    branches: [ main ]
defaults:
  run:
    shell: bash
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        docker_image: ['ros:foxy-ros-base', 'ros:galactic-ros-base', 'ros:humble-ros-base']
    container:
      image: ${{ matrix.docker_image }}
    steps:
    - uses: actions/checkout@v2
    - name: Install rustup dependencies
      run: |
        apt update
        apt install -y curl
    - uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
    - name: cargo test
      run: . /ros_entrypoint.sh && cargo test
