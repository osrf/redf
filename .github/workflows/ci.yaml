name: REDF CI
on:
  pull_request:
  push:
    branches: [ main ]
defaults:
  run:
    shell: bash
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        docker_image: ['ros:galactic-ros-base', 'ros:humble-ros-base']
    container:
      image: ${{ matrix.docker_image }}
    steps:
    - uses: actions/checkout@v2
    - name: Install rustup dependencies
      run: |
        apt update
        apt install -y curl
    - uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
    - name: rosdep
      run: rosdep resolve -q ament_cmake std_msgs example_interfaces rclcpp | sed '/^#/d' | xargs apt install
    - name: cargo test
      run: . /ros_entrypoint.sh && cargo test
